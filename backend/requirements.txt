# Web/API
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27

# Data
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
pyarrow>=16.0       # быстрее CSV/Parquet (опционально)

# Time & parse utils
pendulum>=3.0
python-dateutil>=2.9

# Storage & ORM
SQLAlchemy>=2.0
alembic>=1.13
aiosqlite>=0.20     # если SQLite в async-режиме
# asyncpg>=0.29     # раскомментировать при Postgres

# Settings & utils
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
loguru>=0.7
tenacity>=8.3
orjson>=3.10
rich>=13.7

# Uploads & pagination
aiofiles>=24.1
fastapi-pagination>=0.12
python-multipart>=0.0.20

# Scheduling (по желанию)
APScheduler>=3.10

# Auth (если потребуется JWT)
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3

# Testing & quality (минимум)
pytest>=8.3
pytest-asyncio>=0.23
httpx[http2]>=0.27
ruff>=0.5
black>=24.4
isort>=5.13
