"""
Integration example for GCP Storage Client

This script demonstrates how to use the StorageClient in the HTX application.
It includes examples of uploading, downloading, and managing files in GCS.

Note: This requires actual GCP credentials and a configured bucket to run.
"""

import asyncio
import tempfile
from pathlib import Path
from io import BytesIO

from app.core.config import settings
from app.services.gcp.storage_client import StorageClient, StorageClientError


async def demonstrate_storage_operations():
    """Demonstrate various storage operations"""
    
    # Initialize the storage client
    storage_client = StorageClient(settings)
    
    if not storage_client.is_available:
        print("❌ GCP Storage client is not available.")
        print("Please configure GCP credentials and project settings.")
        return
    
    print("✅ GCP Storage client initialized successfully")
    print(f"📦 Using bucket: {storage_client.bucket_name}")
    
    try:
        # Test 1: Create a sample CSV file and upload it
        print("\\n🔄 Test 1: Upload a sample CSV file")
        sample_csv_data = \"symbol,side,quantity,price,timestamp\\nBTCUSDT,buy,0.001,45000.0,2024-01-01T10:00:00Z\\nETHUSDT,sell,0.05,3000.0,2024-01-01T10:05:00Z\"\n        \n        csv_file = BytesIO(sample_csv_data.encode())\n        csv_file.name = \"sample_trades.csv\"\n        \n        gs_uri = await storage_client.upload_file(\n            csv_file,\n            \"examples/sample_trades.csv\",\n            content_type=\"text/csv\",\n            metadata={\"source\": \"demo\", \"type\": \"trade_data\"}\n        )\n        print(f\"✅ Uploaded file: {gs_uri}\")\n        \n        # Test 2: Check if file exists\n        print(\"\\n🔄 Test 2: Check file existence\")\n        exists = await storage_client.file_exists(\"examples/sample_trades.csv\")\n        print(f\"✅ File exists: {exists}\")\n        \n        # Test 3: Get file metadata\n        print(\"\\n🔄 Test 3: Get file metadata\")\n        metadata = await storage_client.get_file_metadata(\"examples/sample_trades.csv\")\n        if metadata:\n            print(f\"✅ File metadata:\")\n            print(f\"   - Name: {metadata['name']}\")\n            print(f\"   - Size: {metadata['size']} bytes\")\n            print(f\"   - Content Type: {metadata['content_type']}\")\n            print(f\"   - Created: {metadata['created']}\")\n            print(f\"   - Custom Metadata: {metadata['metadata']}\")\n        \n        # Test 4: Generate signed URL\n        print(\"\\n🔄 Test 4: Generate signed URL\")\n        signed_url = await storage_client.generate_signed_url(\n            \"examples/sample_trades.csv\",\n            expiration_minutes=60\n        )\n        print(f\"✅ Signed URL (expires in 1 hour):\")\n        print(f\"   {signed_url}\")\n        \n        # Test 5: List files with prefix\n        print(\"\\n🔄 Test 5: List files in examples/ folder\")\n        files = await storage_client.list_files(prefix=\"examples/\", max_results=10)\n        print(f\"✅ Found {len(files)} files:\")\n        for file_info in files:\n            print(f\"   - {file_info['name']} ({file_info['size']} bytes)\")\n        \n        # Test 6: Download file\n        print(\"\\n🔄 Test 6: Download file\")\n        downloaded_data = BytesIO()\n        success = await storage_client.download_file(\"examples/sample_trades.csv\", downloaded_data)\n        if success:\n            downloaded_data.seek(0)\n            content = downloaded_data.read().decode()\n            print(f\"✅ Downloaded file content:\")\n            print(content[:100] + \"...\" if len(content) > 100 else content)\n        \n        # Test 7: Upload from local file path (if available)\n        print(\"\\n🔄 Test 7: Upload from temporary file\")\n        with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False) as temp_file:\n            temp_file.write('{\"test\": \"data\", \"timestamp\": \"2024-01-01T10:00:00Z\"}')\n            temp_path = Path(temp_file.name)\n        \n        try:\n            json_uri = await storage_client.upload_file(\n                temp_path,\n                \"examples/sample_data.json\",\n                content_type=\"application/json\"\n            )\n            print(f\"✅ Uploaded JSON file: {json_uri}\")\n        finally:\n            # Clean up temporary file\n            temp_path.unlink(missing_ok=True)\n        \n        # Test 8: Cleanup - delete test files\n        print(\"\\n🔄 Test 8: Cleanup test files\")\n        cleanup_files = [\"examples/sample_trades.csv\", \"examples/sample_data.json\"]\n        for file_name in cleanup_files:\n            deleted = await storage_client.delete_file(file_name)\n            print(f\"✅ Deleted {file_name}: {deleted}\")\n        \n        print(\"\\n🎉 All storage operations completed successfully!\")\n        \n    except StorageClientError as e:\n        print(f\"❌ Storage operation failed: {e}\")\n    except Exception as e:\n        print(f\"❌ Unexpected error: {e}\")\n\n\nasync def test_error_handling():\n    \"\"\"Test error handling scenarios\"\"\"\n    print(\"\\n🔧 Testing error handling scenarios\")\n    \n    storage_client = StorageClient(settings)\n    \n    if not storage_client.is_available:\n        print(\"⚠️ Skipping error handling tests - storage client not available\")\n        return\n    \n    try:\n        # Test downloading non-existent file\n        print(\"🔄 Testing download of non-existent file\")\n        success = await storage_client.download_file(\"non-existent-file.csv\", BytesIO())\n        print(f\"✅ Download non-existent file returned: {success}\")\n        \n        # Test file existence for non-existent file\n        print(\"🔄 Testing existence check for non-existent file\")\n        exists = await storage_client.file_exists(\"non-existent-file.csv\")\n        print(f\"✅ File existence check returned: {exists}\")\n        \n        # Test metadata for non-existent file\n        print(\"🔄 Testing metadata retrieval for non-existent file\")\n        metadata = await storage_client.get_file_metadata(\"non-existent-file.csv\")\n        print(f\"✅ Metadata retrieval returned: {metadata}\")\n        \n    except Exception as e:\n        print(f\"❌ Error handling test failed: {e}\")\n\n\ndef check_configuration():\n    \"\"\"Check GCP configuration\"\"\"\n    print(\"🔧 Checking GCP Configuration\")\n    print(f\"   - GCP Project ID: {settings.GCP_PROJECT_ID or 'Not set'}\")\n    print(f\"   - GCP Storage Bucket: {settings.GCP_STORAGE_BUCKET}\")\n    print(f\"   - Credentials Path: {settings.GOOGLE_APPLICATION_CREDENTIALS or 'Not set'}\")\n    print(f\"   - Use Default Credentials: {settings.GCP_USE_DEFAULT_CREDENTIALS}\")\n    print(f\"   - GCP Enabled: {settings.gcp_enabled}\")\n    print(f\"   - GCP Storage Enabled: {settings.gcp_storage_enabled}\")\n    \n    if not settings.gcp_enabled:\n        print(\"\\n⚠️ GCP is not enabled. To enable GCP integration:\")\n        print(\"   1. Set GCP_PROJECT_ID environment variable\")\n        print(\"   2. Set GOOGLE_APPLICATION_CREDENTIALS or GCP_USE_DEFAULT_CREDENTIALS\")\n        print(\"   3. Install GCP dependencies: pip install -r requirements.txt\")\n\n\nasync def main():\n    \"\"\"Main demonstration function\"\"\"\n    print(\"🚀 HTX GCP Storage Client Integration Demo\")\n    print(\"\" + \"=\" * 50)\n    \n    # Check configuration\n    check_configuration()\n    \n    # Run storage operations demo\n    await demonstrate_storage_operations()\n    \n    # Test error handling\n    await test_error_handling()\n    \n    print(\"\\n\" + \"=\" * 50)\n    print(\"✅ Demo completed!\")\n\n\nif __name__ == \"__main__\":\n    # Run the demonstration\n    asyncio.run(main())